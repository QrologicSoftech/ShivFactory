@{
    ViewBag.Title = "Varient";

    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid page-body-wrapper">
    <div class="main-panel">
        <div class="content-wrapper">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-6">
                                    <h4 class="card-title">@ViewBag.Title</h4>
                                </div>
                                <div class="col-6">
                                    <div class="card-description">
                                        @using (Html.BeginForm("AddVarient", "Admin", new { Area = "Admin" }, FormMethod.Get))
                                        {
                                            <p class="card-description" style=" text-align: end;">
                                                <button type="submit" class="btn btn-primary">Add New</button>
                                            </p>
                                        }
                                    </div>
                                </div>
                            </div>
                            <div class="partialView">
                                @{ Html.RenderPartial(Url.Content("VarientPartialView")); }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>